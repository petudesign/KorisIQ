[project]
name = "korisiq"
version = "0.1.0"
description = "Evidence-first ingestion and validation foundation for Finnish basketball data"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
korisiq-ingest = "ingestion.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
